import StadiumExperience from "@/components/hosting/StadiumExperience";

/** Hosting expired cinematic — the only public page while the site is under maintenance. */
const HostingServer = () => <StadiumExperience />;

export default HostingServer;
